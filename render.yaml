services:
  - type: web
    name: iit-dhanbad-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements-render.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MOCK_YOLO
        value: "true"

